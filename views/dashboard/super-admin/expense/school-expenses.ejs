<%- include('../../../partials/head.ejs') %>
<body class="rtl">
<div class="bmd-layout-container bmd-drawer-f-l avam-container animated bmd-drawer-in">
  <%- include('../../../partials/header.ejs') %>
  <div id="dw-s1" class="bmd-layout-drawer bg-faded">
    <%- include('../../../partials/sidebar/index.ejs') %>
  </div>

  <main class="bmd-layout-content">
    <div class="container-fluid">

      <!-- Header -->
      <div class="row m-1 pb-4 mb-3">
        <div class="col-12 p-2">
          <div class="page-header breadcrumb-header d-flex justify-content-between align-items-center">
            <h3 class="lite-text">جميع الصادرات الخاصة بالمدارس</h3>
            <a href="/super-admin/dashboard" class="btn btn-secondary">
              <i class="fas fa-arrow-right"></i> رجوع
            </a>
          </div>
        </div>
      </div>

      <!-- Accordion لكل مدرسة -->
      <div class="accordion" id="schoolsExpensesAccordion">
        <% schools.forEach((school, idx) => { %>
          <div class="card shade mb-2">
            <div class="card-header bg-primary text-white" id="heading<%= idx %>">
              <h5 class="mb-0 d-flex justify-content-between align-items-center">
                <button class="btn btn-link text-white" type="button" data-toggle="collapse" data-target="#collapse<%= idx %>" aria-expanded="false" aria-controls="collapse<%= idx %>">
                  <i class="fas fa-school"></i> <%= school.name %>
                </button>
                <span>المجموع الكلي للصادرات: 
                  <strong><%= (school.expenses.reduce((sum, exp) => sum + exp.amount, 0)).toLocaleString() %></strong>
                </span>
              </h5>
            </div>

            <div id="collapse<%= idx %>" class="collapse" aria-labelledby="heading<%= idx %>" data-parent="#schoolsExpensesAccordion">
              <div class="card-body p-2">

                <!-- بيانات المدرسة -->
                <div class="mb-3">
                  <p><strong>البريد الإلكتروني:</strong> <%= school.email || '—' %></p>
                  <p><strong>الهاتف:</strong> <%= school.phone || '—' %></p>
                  <p><strong>العنوان:</strong> <%= school.address || '—' %></p>
                </div>

                <!-- جدول الصادرات -->
                <div class="table-responsive">
                  <table class="table table-bordered table-hover text-center mb-0">
                    <thead class="thead-light">
                      <tr>
                        <th>#</th>
                        <th>المورد / الجهة</th>
                        <th>المبلغ / الكمية</th>
                        <th>الوصف</th>
                        <th>تاريخ الصادر</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if (!school.expenses || school.expenses.length === 0) { %>
                        <tr><td colspan="5" class="text-muted py-4">لا توجد صادرات لهذه المدرسة</td></tr>
                      <% } else { %>
                        <% school.expenses.forEach((exp, i) => { %>
                          <tr>
                            <td><%= i + 1 %></td>
                         <td><%= exp.incomeId?.supplierId?.name || '—' %></td>
<td><%= exp.amount.toLocaleString() %></td>
<td><%= exp.incomeId?.description || '—' %></td>
<td><%= exp.createdAt.toLocaleDateString() %></td>
                          </tr>
                        <% }) %>
                      <% } %>
                    </tbody>
                  </table>
                </div>

              </div>
            </div>
          </div>
        <% }) %>
      </div>

    </div>
  </main>
</div>

<%- include('../../../partials/scripts.ejs') %>

<script>
  // Optional: expand أول مدرسة افتراضياً
  $('#collapse0').addClass('show');
</script>

</body>
</html>
